let express,mongoose,cors,readdirSync;_102‍.w("express",[["default",["express"],function(v){express=v}]]);_102‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_102‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_102‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);




const morgan = require("morgan");
require("dotenv").config();

const app = express();

// db

mongoose
  .connect(process.env.DATABASE)
  .then(() => console.log("DB connected"))
  .catch((err) => _102‍.g.console.log("DB Connection Error", err));

//   middlewares

app.use(
  express.json({
    limit: "5mb",
  })
);

app.use(express.urlencoded({ extended: true }));

app.use(
  cors({
    origin: ["http://localhost:3000"],
  })
);

// autoload routes
readdirSync('./routes').map((r) => app.use('/api', require(`./routes/${r}`)))

const port = process.env.PORT || 8000;

app.listen(port, () => console.log(`Server running on port ${port}`));
